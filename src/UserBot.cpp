#include "UserBot.h"
#include <list>
using namespace std;

list<Move> UserBot::MakeMoves(Building BuildingPlan)
{
       //Fill the list of moves
       //Start from the next move to make from the hero's current position
        list<Move> moves;

        return moves;
}

